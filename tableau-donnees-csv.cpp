#include <stdexcept>
/** @file **/
#include <iostream>
#include <fstream>
#include <sstream>
#include "tableau-donnees-csv.h"

void afficheTableau(vector<vector<string>> tableau) {
	// Remplacer la ligne suivante par le code adéquat
	throw runtime_error("Fonction afficheTableau non implanté ligne 10");
}

vector<vector<string>> litTableauCSV(string nom_fichier, int nb_colonnes) {
	// Remplacer la ligne suivante par le code adéquat
	throw runtime_error("Fonction litTableauCSV non implanté ligne 15");
}

vector<vector<string>> litTableauCSV(string nom_fichier) {
	// Remplacer la ligne suivante par le code adéquat
	throw runtime_error("Fonction litTableauCSV non implanté ligne 20");
}

vector<string> colonne(vector<vector<string>> t, int i) {
	// Remplacer la ligne suivante par le code adéquat
	throw runtime_error("Fonction colonne non implanté ligne 25");
}

vector<int> conversionInt(vector<string> t) {
	// Remplacer la ligne suivante par le code adéquat
	throw runtime_error("Fonction conversionInt non implanté ligne 30");
}

vector<double> conversionDouble(vector<string> t) {
	// Remplacer la ligne suivante par le code adéquat
	throw runtime_error("Fonction conversionDouble non implanté ligne 35");
}

template<class T>
vector<T> conversion(vector<string> t) {
	// Remplacer la ligne suivante par le code adéquat
	throw runtime_error("Fonction conversion non implanté ligne 41");
}

// Force l'instanciation des templates
template vector<int> conversion<int>(vector<string> t);
template vector<double> conversion<double>(vector<string> t);
